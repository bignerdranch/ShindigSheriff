<%= form_for([@user, @organization]) do |f| %>
<%= f.label :Name %>
<%= f.text_field :name, placeholder: @organization.name %><br/>
<%= f.label :Website %>
<%= f.text_field :website, placeholder: @organization.website %><br/>
<%= f.hidden_field :user_id, placeholder: @current_user %>


  <% if @organization.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@organization.errors.count, "error") %> prohibited this organization from being saved:</h2>

      <ul>
      <% @organization.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

